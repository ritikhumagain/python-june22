#first download git 
#git --version
#make acc in git hub - git hub is an repository
make git.ignore file to ignore that file while push 

# Git commond
git init . -make .git hidden folder to work with
git status - show which is untracked
git add . -a/file name will  show it will add in  git
git commit -m "message" 
    here error may occur global conflict 
    git config --global user.email "username email "
git push origin main 




TO add the repo 
repo clone :git remote add origin and ssh url from git
see status: git remote -v

    


About branch 
git branch = main aauna sakcha
current branch name change : git branch -M "branch name " 
new branch = git branch "new branch name"
delete branch = git branch -D "branch name "
go to branch = git checkout "branch name" 




local
Remote
Remote url
Local repo
Remote repo

ssh-repo 
Public/Private Key
gitignore